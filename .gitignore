__pycache__/
*.py[cod]

# Macro Studio state: recorded macros + key bindings, machine-local
custom/macros.json

# Claude Code local overrides
.claude/settings.local.json

# --- Not redistributed -------------------------------------------------------
# The vendor's MINI KeyBoard app and its decompilation stay local. This repo
# publishes only the independent reimplementation in custom/ and the protocol
# notes derived from it.
*.exe
*.dll
*.pdb
*.application
*.exe.config
*.exe.manifest
app.publish/
en-US/
zh-CN/
custom/decompiled/

# PyInstaller build output. The portable executable is distributed via GitHub Releases
# (pushed by the v* release workflow), not committed here.
custom/build/
custom/dist/

# Local screenshot / scratch dropbox next to the app
custom/temp/
